import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  witch: {
    width: '100%',
    height: '100%',
    position: 'absolute',
    top: 0,
    left: 0,
    zIndex: -1,
  },
});

export default styles;
